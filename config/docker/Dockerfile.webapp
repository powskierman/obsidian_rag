FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose port (Next.js defaults to 3000)
EXPOSE 3000

# Start in development mode for hot-reloading if volumes are mounted
CMD ["npm", "run", "dev"]
