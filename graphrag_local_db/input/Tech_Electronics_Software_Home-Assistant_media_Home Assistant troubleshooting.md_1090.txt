# Home Assistant troubleshooting.md

---
aliases: []
tags:
  - Home-Assistant
  - troubleshooting
---
# Home Assistant troubleshooting
Date: 2023-06-19

### Backlink
[[]]

### Reference
-  
### Notes
- When installing, it's best to connect  
- If no ipv4 address is assigned:
```
network update eth0 --ipv4-method static --ipv4-address 192.168.0.14 --ipv4-gateway 192.168.0.1
``` 
- OTBR: Provide port for rest api
	- The default port for the `otbr-agent` REST API is 8080 and 8081
- Troubleshooting Steps:
	 - Check if pi is running
		 - ping 192.168.0.10
		 - ssh into pi
			 - ssh pi@192.168.0.10
		 - Check if mqtt is at the correct ip
			- Configuration/Integrations/mqtt/re-configure mqtt
				- broker: 192.168.0.10
				- port: 1883
				- username: mlapoint99
				- Password: xlap8911
		- Make sure ports are forwarded
			- Home Assistant
				- 8123, 8234
			- DuckDNS
				- 443, 80
		- If addons not working, reboot completely:
			- completely shutdown the HA (Supervisor > System > shutdown Host), unplug power cable and back in after some seconds to trigger the HA to boot up again.
	- Login via ssh
		- user: root
		- password: same as mac
		- 
- You will not be able to edit anything in Settings > System > General in the UI if you are using YAML configuration for any of the following: name, latitude, longitude, elevation, unit_system, temperature_unit, time_zone, external_url, internal_url."
- undefined reference to `vtable for esphome::status::StatusBinarySensor'
	- change framework from esp-idf to arduino
```
framework:
	type: arduino
	version: recommended
```