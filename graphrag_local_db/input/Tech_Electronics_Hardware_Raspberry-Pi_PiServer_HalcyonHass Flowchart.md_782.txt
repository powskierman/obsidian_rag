# HalcyonHass Flowchart.md

---
aliases: []
tags: []
---
# HalcyonHass Flowchart
Date: 2024-02-15

### Backlink
[[HalcyonHass MoC]]

### Reference
-  
### Notes
```mermaid TI:"Data Flow Flowchart' FOLD
flowchart TD
    ThermostatViewModel["ThermostatViewModel"] -->|uses| HassClientService["HassClientService"]
    WatchManager["WatchManager"] -->|uses| HassClientService
    HassClientService -->|interacts with| HassRestClient["HassRestClient"]

    HassRestClient -->|fetches device state| DeviceState["Device State\n(Decodable)"]
    HassRestClient -->|sends command| CommandResponse["Command Response\n(Decodable)"]
    HassRestClient -->|calls script| ScriptExecutionResult["Script Execution\nResult (Void/Error)"]

    classDef classFill fill:#f9f,stroke:#333,stroke-width:2px;
    class ThermostatViewModel,WatchManager,HassClientService,HassRestClient classFill;

    %% Attempt to reposition notes to avoid parsing errors
    NoteRightOfHassRestClient["HassRestClient handles all direct interactions 
    with the Home Assistant REST API, including authentication,
     data encoding/decoding, and network communication."] --> HassRestClient
    NoteLeftOfThermostatViewModel["ThermostatViewModel is focused on managing
     thermostat-specific states and commands."] --> ThermostatViewModel
    NoteLeftOfWatchManager["WatchManager manages broader aspects of the watch app,
     potentially including thermostat functionalities among others."] --> WatchManager

```