# Compatible.md

---
aliases: []
tags: [
 zephyr
 compatible
]
---
# Compatible
Date: 2023-08-04

### Backlink
[[]]

### Reference
-  
### Notes
-  
Compatible¶

This key is used to match nodes to a binding.  It should look like this in a binding file:

- Note the comma-separated vendor prefix and device name:
	- compatible: "manufacturer,device"
- The devicetree node would match the above binding:
```
device {
     compatible = "manufacturer,device";
};
```

- Assuming no binding has compatible: "manufacturer,device-v2", it would also match this node:
```
device-2 {
    compatible = "manufacturer,device-v2", "manufacturer,device";
};
```

- Each node’s compatible property is tried in order. The first matching binding is used.
- If more than one binding for a compatible is found, an error is raised.