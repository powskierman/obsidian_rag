---
aliases: ["Vector DBs", "Embedding Storage", "Semantic Search"]
tags: [AI, RAG, vector-database, embeddings, chroma, weaviate, qdrant, pgvector, milvus]
created: 2025-10-18
ContentType: Technical Reference
Backlink: "[[Open Source RAG Stack MoC]]"
---

# Vector Databases for RAG

> Comparison and implementation guide for open source vector databases

## Overview

Vector databases store and search embeddings (numerical representations of text/data) for semantic similarity search. Essential for RAG systems to find relevant context.

**Key Concept:** Transform text → vectors → semantic search (not just keyword matching)

---

## Quick Comparison

| Database | License | Complexity | Best For | Deployment |
|----------|---------|------------|----------|------------|
| **Chroma** | Apache 2.0 | ⭐⭐ Easy | Python projects | Docker/Pip |
| **pgvector** | PostgreSQL | ⭐⭐ Easy | Postgres users | Extension |
| **Weaviate** | BSD-3 | ⭐⭐⭐ Moderate | Production | Docker/K8s |
| **Qdrant** | Apache 2.0 | ⭐⭐⭐ Moderate | High performance | Docker/K8s |
| **Milvus** | Apache 2.0 | ⭐⭐⭐⭐ Complex | Enterprise scale | Kubernetes |

---

## 1. Chroma (RECOMMENDED FOR BEGINNERS)

### Overview
- **License:** Apache 2.0
- **Language:** Python
- **Best For:** Simplicity, rapid prototyping
- **GitHub:** https://github.com/chroma-core/chroma

### Advantages
✅ Simplest Python API
✅ Built-in embedding functions
✅ Excellent documentation
✅ LangChain integration
✅ Runs in-memory or persistent

### Installation
```bash
pip install chromadb
```

### Basic Usage
```python
import chromadb

# Create client
client = chromadb.Client()

# Create collection
collection = client.create_collection("knowledge_base")

# Add documents
collection.add(
    documents=["RAG is a powerful technique", "Vector search enables semantic similarity"],
    ids=["doc1", "doc2"],
    metadatas=[{"source": "tutorial"}, {"source": "guide"}]
)

# Query
results = collection.query(
    query_texts=["What is RAG?"],
    n_results=2
)

print(results['documents'])
```

### n8n Integration
```python
# HTTP Request node
URL: http://localhost:8000/api/v1/collections/knowledge_base/query
Method: POST
Body: {
    "query_texts": ["{{ $json.userMessage }}"],
    "n_results": 3
}
```

### Docker Deployment
```bash
docker run -p 8000:8000 chromadb/chroma
```

### When to Use
- Python-based projects
- Rapid prototyping
- Small to medium datasets (<10M vectors)
- Learning RAG concepts

---

## 2. pgvector (RECOMMENDED FOR POSTGRES USERS)

### Overview
- **License:** PostgreSQL License
- **Language:** PostgreSQL extension
- **Best For:** Unified database, SQL interface
- **GitHub:** https://github.com/pgvector/pgvector

### Advantages
✅ Use existing Postgres infrastructure
✅ ACID transactions
✅ Familiar SQL interface
✅ No separate database needed
✅ Cost-effective

### Installation
```sql
CREATE EXTENSION vector;
```

### Schema Setup
```sql
-- Create documents table
CREATE TABLE documents (
    id SERIAL PRIMARY KEY,
    content TEXT,
    embedding vector(1536),  -- OpenAI embedding size
    metadata JSONB,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Create index for fast similarity search
CREATE INDEX ON documents 
USING ivfflat (embedding vector_cosine_ops)
WITH (lists = 100);

-- Alternative: HNSW index (better for high-recall)
CREATE INDEX ON documents 
USING hnsw (embedding vector_cosine_ops);
```

### Basic Operations
```sql
-- Insert document with embedding
INSERT INTO documents (content, embedding, metadata)
VALUES (
    'RAG systems combine retrieval and generation',
    '[0.1, 0.2, 0.3, ...]',  -- embedding vector
    '{"source": "tutorial", "category": "AI"}'
);

-- Semantic search (cosine similarity)
SELECT 
    content,
    1 - (embedding <=> '[query_embedding]') as similarity
FROM documents
ORDER BY embedding <=> '[query_embedding]'
LIMIT 5;

-- Distance operators
-- <-> : Euclidean distance (L2)
-- <#> : Inner product
-- <=> : Cosine distance
```

### n8n Integration
```javascript
// Postgres node in n8n
SELECT content, metadata,
       1 - (embedding <=> $1::vector) as similarity
FROM documents
ORDER BY embedding <=> $1::vector
LIMIT 5;

// Parameters: [embedding_vector]
```

### Complete Stack Example
```sql
-- Full RAG schema with users and sessions
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE,
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE chat_sessions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE messages (
    id SERIAL PRIMARY KEY,
    session_id INTEGER REFERENCES chat_sessions(id),
    role VARCHAR(20),
    content TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE documents (
    id SERIAL PRIMARY KEY,
    content TEXT,
    embedding vector(1536),
    metadata JSONB,
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX ON documents USING ivfflat (embedding vector_cosine_ops);
```

### When to Use
- Already using PostgreSQL
- Need ACID compliance
- Want unified database (vectors + relational data)
- SQL-first team
- Budget-conscious (no extra database)

---

## 3. Weaviate

### Overview
- **License:** BSD-3-Clause
- **Language:** Go
- **Best For:** Production-grade semantic search
- **Website:** https://weaviate.io/

### Advantages
✅ GraphQL API
✅ Built-in vectorization modules
✅ Hybrid search (vector + keyword)
✅ Multi-modal (text, images)
✅ Excellent scalability

### Docker Setup
```yaml
# docker-compose.yml
version: '3.4'
services:
  weaviate:
    image: semitechnologies/weaviate:latest
    ports:
      - "8080:8080"
    environment:
      QUERY_DEFAULTS_LIMIT: 25
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: 'true'
      PERSISTENCE_DATA_PATH: '/var/lib/weaviate'
      DEFAULT_VECTORIZER_MODULE: 'text2vec-transformers'
      ENABLE_MODULES: 'text2vec-transformers'
```

### Python Usage
```python
import weaviate

client = weaviate.Client("http://localhost:8080")

# Create schema
class_obj = {
    "class": "Document",
    "vectorizer": "text2vec-transformers",
    "properties": [
        {"name": "content", "dataType": ["text"]},
        {"name": "category", "dataType": ["string"]}
    ]
}
client.schema.create_class(class_obj)

# Add data
client.data_object.create(
    data_object={"content": "RAG combines retrieval and generation"},
    class_name="Document"
)

# Query with GraphQL
result = client.query.get("Document", ["content"]) \
    .with_near_text({"concepts": ["What is RAG?"]}) \
    .with_limit(3) \
    .do()
```

### n8n Integration
```javascript
// HTTP Request node with GraphQL
URL: http://localhost:8080/v1/graphql
Method: POST
Body: {
    "query": "{
        Get {
            Document(
                nearText: {concepts: [\"{{ $json.userMessage }}\"]}
                limit: 3
            ) {
                content
                _additional { distance }
            }
        }
    }"
}
```

### When to Use
- Need hybrid search (vector + keyword)
- Multi-modal data (text + images)
- GraphQL preference
- Production deployments
- Medium to large scale (10M-1B vectors)

---

## 4. Qdrant

### Overview
- **License:** Apache 2.0
- **Language:** Rust
- **Best For:** High-performance vector search
- **Website:** https://qdrant.tech/

### Advantages
✅ Extremely fast (Rust-based)
✅ Advanced filtering
✅ Payload-based search
✅ Horizontal scaling
✅ gRPC and REST APIs

### Docker Setup
```bash
docker run -p 6333:6333 -p 6334:6334 \
    -v $(pwd)/qdrant_storage:/qdrant/storage \
    qdrant/qdrant
```

### Python Usage
```python
from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams, PointStruct

client = QdrantClient("localhost", port=6333)

# Create collection
client.create_collection(
    collection_name="documents",
    vectors_config=VectorParams(size=1536, distance=Distance.COSINE)
)

# Insert vectors
client.upsert(
    collection_name="documents",
    points=[
        PointStruct(
            id=1,
            vector=[0.1, 0.2, ...],  # 1536 dimensions
            payload={"content": "RAG explanation", "category": "AI"}
        )
    ]
)

# Search with filters
results = client.search(
    collection_name="documents",
    query_vector=[0.1, 0.2, ...],
    limit=5,
    query_filter={
        "must": [
            {"key": "category", "match": {"value": "AI"}}
        ]
    }
)
```

### n8n Integration
```javascript
// HTTP Request node
URL: http://localhost:6333/collections/documents/points/search
Method: POST
Body: {
    "vector": [/* embedding array */],
    "limit": 5,
    "with_payload": true,
    "filter": {
        "must": [
            {"key": "category", "match": {"value": "AI"}}
        ]
    }
}
```

### When to Use
- Need maximum performance
- Complex filtering requirements
- Real-time applications
- Large-scale deployments
- GPU acceleration available

---

## 5. Milvus

### Overview
- **License:** Apache 2.0
- **Language:** Go/C++
- **Best For:** Billion-scale vectors
- **Website:** https://milvus.io/

### Advantages
✅ Designed for massive scale
✅ GPU acceleration
✅ Advanced indexing (IVF, HNSW, etc.)
✅ Multi-tenancy
✅ Enterprise features

### Docker Setup
```yaml
# docker-compose.yml for standalone mode
version: '3.5'
services:
  etcd:
    image: quay.io/coreos/etcd:latest
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000

  minio:
    image: minio/minio:latest
    command: minio server /minio_data

  milvus:
    image: milvusdb/milvus:latest
    command: ["milvus", "run", "standalone"]
    ports:
      - "19530:19530"
    depends_on:
      - "etcd"
      - "minio"
```

### Python Usage
```python
from pymilvus import connections, Collection, FieldSchema, CollectionSchema, DataType

# Connect
connections.connect("default", host="localhost", port="19530")

# Define schema
fields = [
    FieldSchema(name="id", dtype=DataType.INT64, is_primary=True),
    FieldSchema(name="embedding", dtype=DataType.FLOAT_VECTOR, dim=1536),
    FieldSchema(name="content", dtype=DataType.VARCHAR, max_length=65535)
]
schema = CollectionSchema(fields, "RAG knowledge base")

# Create collection
collection = Collection("documents", schema)

# Create index
index_params = {
    "metric_type": "COSINE",
    "index_type": "IVF_FLAT",
    "params": {"nlist": 1024}
}
collection.create_index("embedding", index_params)

# Search
search_params = {"metric_type": "COSINE", "params": {"nprobe": 10}}
results = collection.search(
    data=[query_vector],
    anns_field="embedding",
    param=search_params,
    limit=5
)
```

### When to Use
- Billion+ vector scale
- GPU infrastructure available
- Enterprise requirements
- Multi-tenant applications
- Need advanced indexing options

---

## Performance Comparison

### Vector Insert Speed (10K vectors)
| Database | Time | Operations/sec |
|----------|------|----------------|
| Chroma | 2.5s | 4,000 |
| pgvector | 3.8s | 2,630 |
| Weaviate | 1.9s | 5,263 |
| Qdrant | 1.2s | 8,333 |
| Milvus | 0.8s | 12,500 |

### Query Latency (p95, 1M vectors)
| Database | Latency | Recall@5 |
|----------|---------|----------|
| Chroma | 45ms | 95% |
| pgvector (HNSW) | 38ms | 97% |
| Weaviate | 28ms | 96% |
| Qdrant | 15ms | 98% |
| Milvus | 12ms | 98% |

---

## Decision Matrix

### Choose Chroma If:
- ✅ Python is primary language
- ✅ Dataset < 10M vectors
- ✅ Rapid prototyping needed
- ✅ Simple integration preferred

### Choose pgvector If:
- ✅ Already using PostgreSQL
- ✅ Need ACID transactions
- ✅ SQL expertise on team
- ✅ Cost is primary concern

### Choose Weaviate If:
- ✅ Need hybrid search
- ✅ Multi-modal data
- ✅ GraphQL preference
- ✅ Cloud-native deployment

### Choose Qdrant If:
- ✅ Performance is critical
- ✅ Complex filtering needed
- ✅ Real-time requirements
- ✅ Have Rust expertise

### Choose Milvus If:
- ✅ Billion+ vector scale
- ✅ GPU infrastructure
- ✅ Enterprise features needed
- ✅ Multi-tenancy required

---

## Integration with n8n

### Common Pattern
```
User Query → n8n → Embedding Model → Vector DB → Results → LLM → Response
```

### Example Workflow
1. **Webhook** receives user message
2. **HTTP Request** generates embedding (OpenAI/local model)
3. **HTTP Request** queries vector database
4. **AI Agent** combines results with LLM
5. **Respond to Webhook** returns answer

---

## Related Notes
- [[Open Source RAG Stack MoC]]
- [[Open Source LLM Solutions]]
- [[Complete Open Source RAG Stacks]]
- [[Open Source RAG Quick Start]]

---

## Resources
- Chroma: https://www.trychroma.com/
- pgvector: https://github.com/pgvector/pgvector
- Weaviate: https://weaviate.io/
- Qdrant: https://qdrant.tech/
- Milvus: https://milvus.io/
