---
aliases: 
tags:
  - garage
  - GarageHass
  - REST
  - hassFramework
---
# HassRestClient Flowchart
Date: 2023-12-31

### Backlink
[[]]

### Reference
-  
### Notes
```mermaid TI:Flowchart FOLD
flowchart TB
    init["init()"]
    loadSecrets["loadSecrets()"]
    performRequest["performRequest<T: Decodable>()"]
    fetchDeviceState["fetchDeviceState(deviceId:completion:)"]
    sendCommandToDevice["sendCommandToDevice(deviceId:command:completion:)"]
    callScript["callScript(entityId:completion:)"]
    changeState["changeState(entityId:newState:completion:)"]
    fetchState["fetchState(entityId:completion:)"]
    DecodingErrorExtension["DecodingError Extension\nisEntityNotFoundError()"]
    
    init -->|Load configuration| loadSecrets
    loadSecrets -->|Setup with secrets| performRequest
    performRequest -->|API Request| fetchDeviceState
    performRequest -->|API Request| sendCommandToDevice
    performRequest -->|API Request| callScript
    performRequest -->|API Request| changeState
    performRequest -->|API Request| fetchState
    fetchState -->|Handle DecodingError| DecodingErrorExtension
    
    classDef classFill fill:#f9f,stroke:#333,stroke-width:2px;
    class init,loadSecrets,performRequest,fetchDeviceState,sendCommandToDevice,callScript,changeState,fetchState,DecodingErrorExtension classFill;

```

```mermaid TI:"Flowchart" FOLD
flowchart TD

A[Start] --> B[Initialize HassRestClient<br>with baseURL and authToken]

B --> C{Make Request<br>performRequest}

C -->|GET| D[Fetch Device State<br>fetchDeviceState]

C -->|POST| E[Send Command to Device<br>sendCommandToDevice]

D --> F[Construct GET Request]

E --> G[Construct POST Request<br>Encode Command]

F & G --> H[Perform URLSessionDataTask<br>with Request]

H --> I{Request Successful?}

I -->|Yes| J[Decode Response<br>T: Decodable]

I -->|No| K[Handle Error<br>Network or Decoding Error]

J --> L[Return Success with<br>Decoded Data]

K --> M[Return Failure with<br>Error]

L & M -.-> N[End]

  

style A fill:#f9f,stroke:#333,stroke-width:2px

style N fill:#f9f,stroke:#333,stroke-width:2px
```

```mermaid TI:"Button Flowchart" FOLD
graph TD;
    A(Button Press) -->|isPressed = true| B[handleButtonPress]
    B --> C{entityType}
    C -->|door| D[callScript]
    C -->|alarm| E[showingAlarmConfirmation = true]
    D --> F[WatchManager.shared.handleScriptAction]
    E -->|User confirms| G[toggleAlarmState]
    G --> H[callScript]
    H --> I[WatchManager.shared.handleScriptAction]
    B --> J[Reset Color After 500ms]

    classDef functionName fill:#f9f,stroke:#333,stroke-width:2px;
    class A,B,D,E,F,G,H,J functionName;

```