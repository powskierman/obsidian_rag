---
aliases:
tags:
created: 2022-07-02 10:06
ContentType: Reference
Backlink: ""
---
### Main Idea
- 
### References
- 
### Notes
![Working directory sta (index) repository ](Git-Tutorial-image1.png)

1.  Git repository is a database for versions
2.  Staging area: Place where you acumulate changes for your next commit
3.  Commit: Stores snapshot of project from staging area to git repository
    a.  Before 1st commit, we need to configure user name and email

1.  ![8a31fb Commit Author name and email Commit description](Git-Tutorial-image2.png)

5.  Git init: Initialize new git repository
6.  Git status: Current state of git repository
    a.  Every file in git may have one of four status
        i.  Untracked
        ii. Modified
        iii. Staged
        iv. Unmodified
	Git add: Add files to staging area
	

    a.  moves file from untracked state to staged.

    b.  Usage: git add <fileName>

        i.  Adds changes from working directory to staging area.

        ii. < . > For all

        iii. Git add . -p

             1.  Performs a diff showing changes to edited files before adding

    
    
    ```
    a.  To unstage a file, use 'git reset HEAD <fileName>'




1.  Git diff

    a.  Shows difference between working directory and staging area.

    b.  Git diff --staged is the opposite. It shows what is staged and current commit

        i.  Changes that are about to be committed



```
1.  Git commit: Write changes to git repository

    a.  Git commit -m "Commit Message"

    b.  [Changing a commit message](https://help.github.com/en/github/committing-changes-to-your-project/changing-a-commit-message)



```
1.  Git log

    a.  History of commits

    b.  You can view all commits across all branches by executing git log --all



```
1.  Git checkout: Checkout commit or branch

    a.  git checkout operates on three distinct entities: files, commits, and branches.

    
    
    ```
    a.  Git checkout \<fileName\> Copies \<fileName\> from repository to working directory

    b.  git checkout <commit hash>: Reverts to previous commit

    c.  git checkout <branch> Changes branch


1.  Git rm: remove file from staging area and make it untracked

    a.  Usage: git rm --cached <file>.txt

[git add Stage Untrack](Git-Tutorial-image3.png)



14. ![File states in Git areas Working directory Untracked Modified Unmodified Staging area Stage Unmodified Git repository Unmodified ](Git-Tutorial-image4.png){width="6.947916666666667in" height="3.7916666666666665in"}



```
15. Branch: A text reference to a commit

    a.  Pointers are located in .git/refs/heads

    b.  Current branch tracks any new commits

    c.  Change branch: git checkout <branch>



```
1.  Head: Reference to currently checked out branch or commit

    a.  Detached head: When head points to commit instead of branch



```
1.  ![6fb73b Commit Our very first commit in the project Root 3b95df commit 3b95df Lee Parent HEAD master 44e073 Geumit • Seco*d cot'.mit 31b1e9 31b1e9 Lee filel.txt file2.txt file3.txt Branch file 1 .txt file2.txt ](Git-Tutorial-image5.png){width="6.760416666666667in" height="3.7291666666666665in"}

2.  Git checkout

    a.  Git checkout<branch>: Changes reference to particular branch

    b.  Git checkout <sha1>: Changes reference to particular commit



```
1.  Branches over time:

    a.  ![](Git-Tutorial-image6.png){width="6.59375in" height="3.7916666666666665in"}



```
1.  Git branch

    a.  Lists all local branches

    b.  Git branch <name>: Create new branch

        i.  No spaces in names

    
    
    ```
    a.  Git checkout <name> Checkout specific branch

    b.  Git checkout -b <name> Create and checkout new branch

    c.  Git branch -d <name>: Delete branch

        i.  Need to switch to other branch first

    
    
    ```
    a.  Git branch -m <oldName> <newName>: Rename branch

    b.  Git branch -v : List information on branches

    c.  Git branch -a: List info on all branches; local and remote.



```
1.  Git merge

    a.  Merge 2 branches

    b.  Two types

        i.  Fast-forward

        ii. 3-way merge

    
    
    ```
    a.  Resolve conflicts

        i.  Easiest in VS Code



```
1.  Github

    a.  [Add new remote repository](https://help.github.com/en/github/importing-your-projects-to-github/adding-an-existing-project-to-github-using-the-command-line) from existing local repository

    b.  Git clone <URL>: Retrieve git repository from github

        i.  At the top of your GitHub repository's Quick Setup page, click to copy the remote repository URL

    
    
    ```
    a.  Git remote

        i.  Git remote -v : verbose

            1.  List all remote servers for local git repository

        
        
        ```
        i.  git remote add origin <remote repository URL>

            1.  At the top of your GitHub repository's Quick Setup page, click to copy the remote repository URL

            2.  Adds local repository to remote github server (origin)

        
        
        ```
        i.  Git remote show <remoteServer>: Provides detailed information on branches and tracking of remote repository from local repository

            1.  Origin is default name for remote server

        
        
        ```
        i.  Git remote prune <remoteServer>: Empty github trash of deleted branch in <remoteServer>

    
    
    ```
    a.  Git branch

        i.  Tracking branch is local branch with same name as remote branch

        ii. -a: List all local and remote branches

        iii. -r: List remote branches

        iv. -vv: List information on tracking and tracked branches



5.  Git push, fetch and pull

6.  ![Overview of the push, fe n ey•actlon with remote Git repository Working directory Stage git add Staging area (index) Git repository Com "it git c mmit Jump into certain "version" of the project git checkout Remote Git repository Push to remote git push Fetch updates it fetch Merge changes from remote repository ait Dull ](Git-Tutorial-image7.png){width="5.958333333333333in" height="3.7708333333333335in"}

7.  Git pull

    a.  2 step process

        i.  Git fetch

        ii. Git merge



```
1.  ![Git pull - merges remote branch into current branch Working directory updated Staging area (index) Git reposito git fetch Remote Git repository git merge FETCH_HEAD updated ](Git-Tutorial-image8.png){width="5.947916666666667in" height="2.7916666666666665in"}

    a.  Checkout local branch

    b.  Make sure it is a tracking branch: git branch -vv

    c.  Enter git pull:

        i.  git fetch executed in background

            1.  Fetches all changes in remote repository

        
        
        ```
        i.  Git fetches FETCH_HEAD file

            1.  Contains SHA1 of last remote commits for all tracking branches

        
        
        ```
        i.  Git executes git merge FETCH_HEAD

        ii. Git merges remote branch into current local branch



```
1.  Git push

    a.  Enter as is to push changes to remote

    b.  -v: verbose

    c.  Git push -u <remoteServer> <newBranch>: create remote branch from new local branch

        i.  <remoteServer> is usually 'origin'

    
    
    ```
    a.  Git push -d <remoteServer><newBranch>: deletes remote branch



```
1.  Git remote

    a.  Git remote update <remoteServer> --prune

        i.  Update tracking status after deleting remote branch



```
1.  Git show-ref

    a.  Shows all references local and remote with sha1

    b.  Git show-ref <branch>: Shows references for specific branch



```
23. Under the Hood

    a.  Git hash-object: Create new blob

        i.  Usage: echo "Hello, Git" | git hash-object --stdin -w

            1.  git hash-object toto.txt -w



2.  Shasum: Generate sha1

3.  Find:

    a.  Usage: find .git/objects -type f (Find objects of type file)

        i.  ![Content Git Object Object Object Type Length Hash Delimiter blob: 11 Hello, Git ](media/Git-Tutorial-image9.png){width="6.5in" height="3.59375in"}

Note: Content comes last and not first in hash generation. 0 is null separator.

Example: echo "blob 300Second file in Git repository" | shasum



4.  Git mktree:

    a.  Usage: <file>.txt | git mktree

    b.  Requires file with specific layout:

        i.  Permission/type/hash/fileName. Note: fields are separated by spaces except for fileName, which is preceded by <tab>

        ii. 100644 blob 0527e6bd2d76b45e2933183f1b506c7ac49f5872file0.txt

        iii. 100644 blob b7aec520dec0a7516c18eb4c68b64ae1eb9b5a5efile1.txt

        iv. 100644 blob 4400aae52a27341314f423095846b1f215a7cf08file1.txt



```
1.  Git read-tree <hash>

    a.  Transfers files from repository to staging area



```
1.  Git cat-file:

    a.  Usage: git - p cat-file <hash>: Print content of object

        i.  git - t cat-file <hash>: Display type of object

        ii. git - s cat-file <hash>: Display size of object

        iii. Note: 1st 4 characters of <hash> are sufficient



```
1.  Git ls-files list git files in working tree and staging area

    a.  Parameters: <https://git-scm.com/docs/git-ls-files>



```
8.  Git unpack: Used to unpack pack file

    a.  move the pack objects outside the .git/objects/pack directory

    b.  The pack files need to be inside the repository.

        i.  From <<https://stackoverflow.com/questions/16972031/how-to-unpack-all-objects-of-a-git-repository>>

    
    
    ```
    a.  cat <pack-.pack> | git unpack-objects



```
9.  Git config: Used to configure user name and email

    a.  Git config --global user.name <name>

    b.  Git config --global user.email <email>

    c.  Git config --global color.ui=true

    d.  Git config --list

    
    
    ```
    e.  git config credential.helper store : Stores username and password for push and pull commands

        i.  From <<https://superuser.com/questions/338511/how-do-i-disable-password-prompts-when-doing-git-push-pull>>



```
1.  Miscellaneous:

    a.  Make Kicad compatible with git

        i.  <https://jnavila.github.io/plotkicadsch/>













a.
### Related Notes
- 
### Questions / Ideas for Further Exploration
- 
### To-Do
- 
### Smart Connections Insights
- 
