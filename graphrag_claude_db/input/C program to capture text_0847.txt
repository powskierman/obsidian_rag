---
aliases: []
tags: []
---
# C program to capture text
Date: 2023-09-17

### Backlink
[[]]

### Reference
-  
### Notes
```C
#include <stdio.h> 
#include "freertos/FreeRTOS.h" 
#include "freertos/task.h"

void app_main (void)
char c = 0;
char str [100];
memset (str,o,sizeof (str));
while(c!= 'In')
{
c = getchar ();
if(c != Oxff)
T
str[strlen(str)] = c;
printf ("%c",c);
}
vTaskDelay(100 / portTICK_PERIOD_MS);
}
printf("you typed: %s\n", str);
}
```