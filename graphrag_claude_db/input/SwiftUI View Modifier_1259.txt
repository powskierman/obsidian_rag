---
aliases: []
---
# SwiftUI View Modifier
Date: 2022-10-09

### Tags: 

### Backlink
[[]]

### Reference
- [SwiftUI View - viewDidLoad()?](https://stackoverflow.com/questions/56496359/swiftui-view-viewdidload)
- 
### Notes
-  We can achieve this using  a view modifier. (See Garage app)

1.  Create `ViewModifier`:

```swift
struct ViewDidLoadModifier: ViewModifier {

    @State private var didLoad = false
    private let action: (() -> Void)?

    init(perform action: (() -> Void)? = nil) {
        self.action = action
    }

    func body(content: Content) -> some View {
        content.onAppear {
            if didLoad == false {
                didLoad = true
                action?()
            }
        }
    }

}
```

2.  Create `View` extension:

```swift
extension View {

    func onLoad(perform action: (() -> Void)? = nil) -> some View {
        modifier(ViewDidLoadModifier(perform: action))
    }

}
```

3.  Use like this:

```swift
struct SomeView: View {
    var body: some View {
        VStack {
            Text("HELLO!")
        }.onLoad {
            print("onLoad")
        }
    }
}
```