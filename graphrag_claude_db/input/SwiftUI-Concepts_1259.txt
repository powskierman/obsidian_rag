---
aliases: []
---

2022-06-25
# SwiftUI-Concepts

### Backlink
[[../../../../Programming-Tools/SwiftUI/SwiftUI MoC]]

### Reference

### Notes
- Structs instead of Classes

    a.  UIKit or AppKit use*classes*for views rather than structs.

    b.  structs are simpler and faster than classes.

    c.  In SwiftUI, all our views are trivial structs

        i.  No inheritance

```{=html}
<!-- -->
```
1.  ContentView



1.  Views are a function of their state

    a.  Previously apps could only store sequence of events not states.

        i.  All settings had to be redone to return to a state

    ```{=html}
    <!-- -->
    ```
    a.  State is the result of all of the settings at a given point.

    b.  A field can only show something if it reflects a value stored in your program.

    c.  Everything the user can see is just the visible representation of the structs and properties in our code.

```{=html}
<!-- -->
```
1.  there is nothing behind our view

    a.  If we want to fill the screen change frame()

        i.  Pass in .infinity

        ii. Text("Hello World")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color.red)

        iii. By default your view won't leave the safe area, but you can change that by using the**edgesIgnoringSafeArea()**modifier

             i.  .edgesIgnoringSafeArea(.all)

```{=html}
<!-- -->
```
1.  Modifier order matters

    a.  Types

        i.  Layout modifiers

        ii. Effects

        iii. text

        iv. events

    ```{=html}
    <!-- -->
    ```
    a.  Whenever we apply a modifier, we create a new view with that change

    b.  Each modifier creates a new struct with that modifier applied

    c.  Swift's**type(of:)**method prints the exact type of a particular value

        i.  print(type(of: **self**.body))

    ```{=html}
    <!-- -->
    ```
    a.  We can apply the same effect multiple times

    b.  You can chain modifiers

```{=html}
<!-- -->
```
1.  primitive views

    a.  Layout

        i.  Stacks

        ii. Spacer

    ```{=html}
    <!-- -->
    ```
    a.  Control

        i.  Form

        ii. List

        iii. Datepicker

        iv. Button
 
-  