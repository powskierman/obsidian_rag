---
aliases: 
tags:
  - thermoX
  - gazebo
  - class
---
# thermoX class diagram
Date: 2024-09-01

### Backlink
[[GazeboWX MoC]]

### Reference
-  
### Notes
```mermaid
classDiagram
    ThermoX --> BlynkModule
    ThermoX --> Thermostat
    ThermoX --> WiFiManager
    ThermoX --> NTPServer
    ThermoX --> OTAUpdater
    ThermoX --> WeatherForecast
    Thermostat --> DHT22Sensor
    Thermostat --> RelayControl

    class ThermoX {
        -BlynkTimer timer
        +setup()
        +loop()
        +checkWifi()
        +TempUpdate()
        +TimerStatus()
        +Fan(boolean)
        +KillManual()
        +intervals()
    }

    class BlynkModule {
        +BlynkInit()
    }

    class Thermostat {
        -float TempDes
        -float TempAvg
        -int RelayPin
        +GetPresets()
        +MenuReset()
    }

    class WiFiManager {
        +connectWifi()
    }

    class NTPServer {
        +getTime()
    }

    class OTAUpdater {
        +ota()
    }

    class WeatherForecast {
        +printCurrentWeather()
    }

    class DHT22Sensor {
        +readTemperature()
        +readHumidity()
    }

    class RelayControl {
        +turnOn()
        +turnOff()
    }
```