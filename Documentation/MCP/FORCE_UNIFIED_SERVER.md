# Force Unified MCP Server

If multiple MCP servers are configured, disable the extras and keep the unified server only.

## Steps

1. Edit your MCP config file (Claude Desktop settings).
2. Keep the unified server entry and remove/disable the others.
3. Restart Claude Desktop.

Use `Documentation/MCP/MCP_CAPABILITIES_COMPARISON.md` if you need to compare servers before disabling.
