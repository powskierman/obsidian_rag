# Root Directory Cleanup Analysis

**Analysis Date**: December 28, 2025
**Status**: 🧹 Ready for Cleanup
**Total Space Reclaimable**: ~12.5 GB

---

## Executive Summary

The `obsidian_rag` root directory contains significant redundant files and directories that can be safely removed:

- **11 GB** in graph_data (likely safe to delete most)
- **569 MB** in old database backups
- **5 MB** in test coverage HTML reports
- **Multiple log files** totaling several hundred MB
- **Duplicate configuration files**
- **Obsolete Python scripts**
- **Git conflict markers** in .gitignore

---

## Category 1: Large Directories (Safe to Delete)

### 1. Test Coverage Reports (5 MB)
```
htmlcov/                    5.0M
.coverage                   52K
coverage.xml                96K
```

**Recommendation**: ✅ **DELETE**
- Generated by pytest coverage
- Can be regenerated anytime with `pytest --cov`
- Already in .gitignore (line 55)
- No impact on functionality

**Command**:
```bash
rm -rf htmlcov/ .coverage coverage.xml
```

### 2. Old Database Backups (569 MB)
```
chroma_db.backup_20251119_151324/          73M    (Nov 27 backup)
chroma_db_backup_20251107_112029/         184M    (Nov 7 backup)
lightrag_db.old_20251120_133547/          141M    (Nov 22 backup)
lightrag_db_backup_20251102_223041/       283M    (Nov 2 backup)
lightrag_db_backup_nomic_768/             145M    (Nov 22 backup)
```

**Current Active Databases**:
- `chroma_db/` (63M) - Current vector database
- `lightrag_db/` (152M) - Current LightRAG database

**Recommendation**: ✅ **DELETE OLD BACKUPS**
- Current databases are working
- Backups are over 1 month old
- Can rebuild from source if needed
- All patterns already in .gitignore (lines 45, 57)

**Commands**:
```bash
rm -rf chroma_db.backup_20251119_151324/
rm -rf chroma_db_backup_20251107_112029/
rm -rf lightrag_db.old_20251120_133547/
rm -rf lightrag_db_backup_20251102_223041/
rm -rf lightrag_db_backup_nomic_768/
```

### 3. GraphRAG Databases (Experimental/Unused - 55 MB)
```
graphrag_claude_db/        32M
graphrag_db/              160K
graphrag_gpt_oss_db/       92K
graphrag_local_db/         23M
```

**Status**: Appears to be experimental/testing databases

**Recommendation**: ⚠️ **CONFIRM BEFORE DELETE**
- Check if these are actively used
- If not used, safe to delete
- Already in .gitignore (lines 52-54)

**Commands** (if confirmed unused):
```bash
rm -rf graphrag_claude_db/
rm -rf graphrag_db/
rm -rf graphrag_gpt_oss_db/
rm -rf graphrag_local_db/
```

### 4. Graph Data Directory (11 GB)
```
graph_data/                11G
```

**Contains**: NetworkX graph pickle files (23,926 nodes, 35,030 edges)

**Recommendation**: ⚠️ **KEEP BUT REVIEW**
- This is the main knowledge graph
- CRITICAL for graph-based queries
- However, 11GB seems very large
- Consider:
  - Check if there are old graph versions that can be deleted
  - Only keep latest graph pickle files
  - Remove any temporary/intermediate files

**Investigation**:
```bash
# Check what's inside
ls -lh graph_data/ | head -20
du -sh graph_data/*/ 2>/dev/null
```

---

## Category 2: Log Files (Safe to Delete)

```
debug_service.log              748B    (Dec 25)
debug_service_2.log           255K    (Dec 26)
embedding_service.log            0B    (Nov 21)
graph_service.log             271B    (Dec 24)
streamlit.log                  52B    (Nov 21)
reproduction_output.log       2.1K    (Nov 24)
```

**Recommendation**: ✅ **DELETE ALL**
- Temporary debugging logs
- Can be regenerated as needed
- Already in .gitignore (line 17: `*.log`)

**Command**:
```bash
rm -f *.log
```

---

## Category 3: Gitleaks Reports (Safe to Delete)

```
gitleaks-report 2.json        135K    (Dec 22)
gitleaks-report 3.json        135K    (Dec 22)
```

**Recommendation**: ✅ **DELETE**
- Security scan reports (duplicates with " 2" and " 3" suffixes)
- Old scan results
- Can regenerate with `gitleaks detect`
- Already in .gitignore (line 16: `*.json`)

**Command**:
```bash
rm -f "gitleaks-report 2.json" "gitleaks-report 3.json"
```

---

## Category 4: Duplicate/Obsolete Configuration Files

### JSON Files
```
ADD_TO_CLAUDE_CONFIG.json                  431B   (Nov 7)
claude_desktop_config_with_obsidian.json  2.5K   (Nov 22)
```

**Recommendation**: ⚠️ **REVIEW BEFORE DELETE**
- Check if these are still needed for setup documentation
- If just examples, move to Documentation/examples/
- `claude_desktop_config_with_obsidian.json` already in .gitignore (line 90)

### Gitleaks Config Files
```
.gitleaks 2.toml              373B   (Dec 22)
.gitleaks.toml                116B   (Dec 23)
.gitleaks.toml                373B   (Dec 23) - SPACE IN FILENAME!
```

**Issue**: Three gitleaks config files, including one with a trailing space!

**Recommendation**: ✅ **KEEP ONE, DELETE DUPLICATES**
- Keep `.gitleaks.toml` (116B, most recent)
- Delete `.gitleaks 2.toml` (duplicate)
- Delete `.gitleaks.toml ` (trailing space - problematic!)

**Commands**:
```bash
rm -f ".gitleaks 2.toml"
rm -f ".gitleaks.toml "
# Keep: .gitleaks.toml
```

---

## Category 5: Obsolete Python Scripts

```
obsidian_rag_mcp_fixed.py       8.3K   (Nov 21)
obsidian_rag_unified_mcp.py     8.3K   (Nov 22)
openrouter_client.py            372B   (Dec 23)
test_openrouter.py              955B   (Dec 23)
```

**Recommendation**: ⚠️ **REVIEW**
- `obsidian_rag_unified_mcp.py` already in .gitignore (line 70)
- Check if these are:
  - Replaced by code in `src/`
  - One-off test scripts
  - Still needed for setup/configuration

**If obsolete, move to Archive** or delete

---

## Category 6: Shell Scripts

```
run.sh                 2.3K   (Nov 21)
run_claude_index.sh    380B   (Nov 21)
save_notes.sh          9.2K   (Nov 21)
setup.sh               5.2K   (Nov 21)
search_vault           335B   (Nov 9)
```

**Recommendation**: ⚠️ **KEEP BUT ORGANIZE**
- These appear to be active utility scripts
- Consider moving to `Scripts/` directory for organization
- Review if they're still used or superseded by Docker setup

---

## Category 7: Python Cache (Safe to Delete)

```
__pycache__/           276K
.pytest_cache/          28K
```

**Recommendation**: ✅ **DELETE**
- Python bytecode cache
- Regenerated automatically
- Already in .gitignore (lines 43, 38)

**Command**:
```bash
rm -rf __pycache__/ .pytest_cache/
```

---

## Category 8: .gitignore Issues

### Git Conflict Markers (Lines 91-98)
```gitignore
<<<<<<< Updated upstream
webapp/
webapp/.next/
webapp/node_modules/
webapp/.turbo/
=======
webapp/.next/
>>>>>>> Stashed changes
```

**Recommendation**: ✅ **FIX IMMEDIATELY**
- Git merge conflict markers left in file
- Should be resolved

**Fix**:
```gitignore
# Webapp build artifacts
webapp/.next/
webapp/node_modules/
webapp/.turbo/
webapp/dist/
```

---

## Cleanup Script

Here's a safe cleanup script that handles the obvious items:

```bash
#!/bin/bash
# cleanup_root.sh - Safe cleanup of obsidian_rag root directory

set -e

echo "🧹 Obsidian RAG Root Directory Cleanup"
echo "======================================"
echo

# Change to project root
cd "/Users/michel/Library/Mobile Documents/com~apple~CloudDocs/ai/RAG/obsidian_rag"

# 1. Delete test coverage files (5 MB)
echo "1. Removing test coverage files..."
rm -rf htmlcov/ .coverage coverage.xml
echo "   ✅ Removed test coverage files"

# 2. Delete log files
echo "2. Removing log files..."
rm -f *.log
echo "   ✅ Removed log files"

# 3. Delete gitleaks duplicate reports
echo "3. Removing duplicate gitleaks reports..."
rm -f "gitleaks-report 2.json" "gitleaks-report 3.json"
echo "   ✅ Removed gitleaks reports"

# 4. Delete gitleaks config duplicates
echo "4. Removing duplicate gitleaks config files..."
rm -f ".gitleaks 2.toml"
rm -f ".gitleaks.toml "  # Note the trailing space
echo "   ✅ Removed duplicate gitleaks configs"

# 5. Delete Python cache
echo "5. Removing Python cache directories..."
rm -rf __pycache__/ .pytest_cache/
echo "   ✅ Removed Python cache"

# 6. Delete old database backups (569 MB)
echo "6. Removing old database backups..."
rm -rf chroma_db.backup_20251119_151324/
rm -rf chroma_db_backup_20251107_112029/
rm -rf lightrag_db.old_20251120_133547/
rm -rf lightrag_db_backup_20251102_223041/
rm -rf lightrag_db_backup_nomic_768/
echo "   ✅ Removed old database backups (569 MB freed)"

echo
echo "✅ Cleanup complete!"
echo
echo "Space freed: ~574 MB"
echo
echo "⚠️  Manual review needed for:"
echo "   - graph_data/ (11 GB - check for old versions)"
echo "   - graphrag_*_db/ directories (55 MB - confirm if unused)"
echo "   - Python scripts in root (check if obsolete)"
echo "   - .gitignore (fix git conflict markers)"
echo
```

---

## Manual Review Required

### 1. Graph Data Directory (11 GB)
```bash
# Investigate what's taking up space
cd graph_data/
du -sh * | sort -hr | head -20

# Look for old versions or temporary files
ls -lht | head -20
```

**Action**: Delete old graph versions, keep only latest

### 2. GraphRAG Databases (55 MB)
**Question**: Are these still used or experimental?

**Check**:
```bash
grep -r "graphrag_" src/ config/ --include="*.py" --include="*.yml"
```

If no references found, safe to delete.

### 3. Root Python Scripts
**Review each**:
- `obsidian_rag_mcp_fixed.py` - Still needed?
- `obsidian_rag_unified_mcp.py` - Already in .gitignore, delete?
- `openrouter_client.py` - Still used or moved to src/?
- `test_openrouter.py` - One-off test, archive?

### 4. Shell Scripts
**Consider organizing**:
```bash
# Move utility scripts to Scripts/ directory
mv run.sh Scripts/
mv run_claude_index.sh Scripts/
mv save_notes.sh Scripts/
mv setup.sh Scripts/
mv search_vault Scripts/
```

---

## Immediate Actions (Safe)

Can be done right now without review:

```bash
# Quick cleanup (no data loss risk)
cd "/Users/michel/Library/Mobile Documents/com~apple~CloudDocs/ai/RAG/obsidian_rag"

# Delete obvious temporary files
rm -rf htmlcov/ .coverage coverage.xml __pycache__/ .pytest_cache/
rm -f *.log
rm -f "gitleaks-report 2.json" "gitleaks-report 3.json"
rm -f ".gitleaks 2.toml" ".gitleaks.toml "

# Delete old database backups (over 1 month old)
rm -rf chroma_db.backup_20251119_151324/
rm -rf chroma_db_backup_20251107_112029/
rm -rf lightrag_db.old_20251120_133547/
rm -rf lightrag_db_backup_20251102_223041/
rm -rf lightrag_db_backup_nomic_768/

echo "✅ Safe cleanup complete! ~574 MB freed"
```

---

## .gitignore Fix

Replace lines 91-98 with:

```gitignore
# Webapp build artifacts
webapp/.next/
webapp/node_modules/
webapp/.turbo/
webapp/dist/
```

---

## Summary

### Safe to Delete Immediately (~574 MB)
- ✅ Test coverage files (5 MB)
- ✅ Log files (~256 KB)
- ✅ Gitleaks duplicates (~270 KB)
- ✅ Python cache (304 KB)
- ✅ Old database backups (569 MB)

### Needs Review Before Delete (~11+ GB)
- ⚠️ graph_data/ (11 GB) - Check for old versions
- ⚠️ graphrag_*_db/ (55 MB) - Confirm if unused
- ⚠️ Root Python scripts - Check if obsolete
- ⚠️ Config JSON files - Check if still needed

### Fix Required
- ⚠️ .gitignore git conflict markers (lines 91-98)

### Reorganize (Optional)
- Shell scripts → Scripts/
- Example configs → Documentation/examples/

---

## Total Space Reclaimable

- **Immediate**: ~574 MB (safe cleanup)
- **With Review**: 11-12 GB (if graph_data old versions removed)
- **Total Potential**: ~12.5 GB

---

## Recommendations Priority

**Priority 1 (Do Now)**:
1. Run safe cleanup script above
2. Fix .gitignore conflict markers
3. Delete duplicate .gitleaks.toml with trailing space

**Priority 2 (Review This Week)**:
1. Investigate graph_data/ directory
2. Confirm graphrag databases are unused
3. Review root Python scripts

**Priority 3 (Future Cleanup)**:
1. Move shell scripts to Scripts/
2. Organize example configs
3. Set up automated cleanup (e.g., delete logs older than 30 days)

---

## Files to Keep (Important!)

**DO NOT DELETE**:
- `.env` - Environment variables
- `.gitignore` - Git configuration
- `docker-compose.yml` - Docker configuration (symlink)
- `Dockerfile`, `Dockerfile.lightrag` - Container definitions
- `requirements*.txt` - Python dependencies
- `pytest.ini` - Test configuration
- `README.md` - Documentation
- `Launch Obsidian RAG.command` - macOS launcher
- Current databases: `chroma_db/`, `lightrag_db/`
- `graph_data/` - Main knowledge graph (but clean old versions)
- Active directories: `src/`, `config/`, `Scripts/`, `Documentation/`, `webapp/`

---

## Conclusion

The root directory has significant cleanup potential, with at least **574 MB** safely removable immediately, and potentially **12+ GB** with proper review of the graph_data directory.

The most impactful actions are:
1. Delete old database backups (569 MB)
2. Fix .gitignore conflict markers
3. Review graph_data/ for old versions (11 GB)
4. Clean up duplicate configuration files

This will make the repository cleaner, easier to navigate, and more efficient for iCloud sync.
